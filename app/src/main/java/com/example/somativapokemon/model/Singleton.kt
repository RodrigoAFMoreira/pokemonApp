package com.example.somativapokemon.model

object Singleton {
   /* lateinit var citiesArray:List<pokemon>
    lateinit var dao: pokemonDAO
    fun setContext(context: Context){
        pokemonDatabase.getInstance(context)?.apply {
            dao = pokemonDAO
            citiesArray = dao.getAll()
        }
    }
    fun addCity(city: pokemon){
        dao.insertAll(city)
        citiesArray = dao.getAll()
    }
    fun updateCity(city: pokemon){
        dao.updatePokemon(city)
        citiesArray = dao.getAll()
    }
    fun deleteCity(city: pokemon){
        dao.deletePokemon(city)
        citiesArray = dao.getAll()
    } */
}